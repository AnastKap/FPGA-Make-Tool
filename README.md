# FPGA Makefile Toolchain

This repository contains the basic files to automate the procedure of FPGA testing and kernel implementation

## How to use?
Copy in your workspace folder (where you wish to run the *make* command) the file under the name *template_settings.mk* and rename it to *settings.mk*. Then, make the appropriate changes to the variables and, after that, you are ready to run the tool.